# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Model Configuration
# For text generation (recipe creation) - cost-effective option
OPENAI_MODEL=gpt-4o-mini

# Note: For image analysis, gpt-4o is used automatically regardless of this setting
# as it's the only OpenAI model that supports vision capabilities

# Optional: Debug mode
# Uncomment to enable debug logging
# DEBUG=true
